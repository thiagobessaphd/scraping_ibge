services:
  scraper-ibge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scraper-ibge-municipios
    init: true
    shm_size: "1gb"
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./resultados_ibge:/app/resultados_ibge
    restart: "no"
